// Shared by StepShell's slide and Background's dither parallax so the two
// stay in lockstep -- change one, change both.
export const STEP_DURATION = 280
export const STEP_EASE = 'cubic-bezier(0.23, 1, 0.32, 1)'
